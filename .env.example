# Environment: "production" or "development"
# Production: Enables all security features (HTTPS, secure cookies, HSTS, CSP)
# Development: Disables security features, uses localhost automatically
ENVIRONMENT=production

# Your domain (required for production, ignored in development)
# Single domain: DOMAINS=example.com
# Multiple domains: DOMAINS=example.com,www.example.com
DOMAINS=example.com

# Optional: Enable Django admin interface (disabled by default for security)
# ENABLE_DJANGO_ADMIN=true