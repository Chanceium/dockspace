services:
  dockspace:
    image: ghcr.io/chanceium/dockspace:latest
    ports:
      - "8000:8000"
    volumes:
      # Persist SQLite DB and media uploads in a single host directory
      - ./data:/data
      # Mail server config (adjust path as needed)
      - /docker-mail/config:/dms
    restart: unless-stopped
